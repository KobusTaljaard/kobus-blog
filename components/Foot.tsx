import Subscribe from './Subscribe';

/** End of a post: subscribe only (comments sit above). */
export default function Foot() {
  return (
    <footer className="foot wrap">
      <Subscribe />
    </footer>
  );
}
